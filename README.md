# Luna GitHub Pages website — fixed download

Upload **every file in this folder** to the root of your existing GitHub Pages repository, then click **Commit changes**.

Files to upload:
- `index.html`
- `.nojekyll`
- `README.md`
- `Luna.zip`

Important: `Luna.zip` is intentionally in the **same folder as `index.html`**. The Download button points to `./Luna.zip`, which avoids the missing-file problem caused by the previous `downloads/` folder setup.

This package already contains the newest Luna build with:
- Publisher: Luna Founder
- Wake phrase: Hey Luna AI
